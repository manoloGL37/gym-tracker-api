app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:https://gym-tracker-eight-dun.vercel.app}
  auth:
    cookie-secure: ${REFRESH_COOKIE_SECURE:true}
    cookie-same-site: ${REFRESH_COOKIE_SAME_SITE:None}
